export enum ActivityType {
    Income,
    Saving,
    Expenditure
}

export enum PaymentType {
    Cash,
    CreditCard,
    GiroCard,
    Paypal
}

export enum FrequencyType {
    Daily,
    Monthly,
    Yearly
}
